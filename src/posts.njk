---
layout: default
title: GreM@Pro - IT and Passion
htmlClass: html
bodyClass: body

---

<div class="container">

  <h1> {{ site.name }} </h1>

  <h2> I miei appunti tecnici </h2>

  </br>



 <ul class="list-group">
    {%- for post in collections.post | reverse -%}
      <li class="list-group-item d-flex justify-content-between align-items-center">
       {% if loop.index0 < 10 %}
          <a href={{ post.url }}><i class="{{ post.data.faicon }}"></i> {{ post.data.title }} </a>
      {% else%}
        <a href={{ post.url }}>{{ post.data.title }} </a>
      {% endif%}

        <span class="badge badge-primary badge-pill">{{ post.data.date }}</span>
      </li>
    {%- endfor -%}
  </ul>




</div>
